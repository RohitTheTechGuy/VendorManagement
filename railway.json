{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "CHECKPOINT_DISABLE=1 npx prisma migrate deploy --schema=packages/db/prisma/schema.prisma && echo '>>> MIGRATE_DONE STARTING_NODE <<<' && node apps/api/dist/index.js",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
